<%= form_for(@promo) do |f| %>
  <% if @promo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promo.errors.count, "error") %> prohibited this promo from being saved:</h2>

      <ul>
      <% @promo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :imagen_promo %><br>
    <%= f.file_field :imagen_promo %>
  </div>
  <div class="field">
    <%= f.label :disponible %><br>
    <%= f.check_box :disponible %>
  </div>
  <div class="actions">
    <%= f.submit class: "button button_purple" %>
  </div>
<% end %>
